-- data/sample.sql
-- เทมเพลต seed แบบ SQL (ทางเลือก)
-- คุณสามารถรันด้วย psql:
--   psql "postgresql://postgres:postgres@localhost:5432/icn_freeze" -f data/sample.sql

BEGIN;

-- ตัวอย่าง:
-- INSERT INTO "Category"("id","name") VALUES (1,'Shirt');

COMMIT;
