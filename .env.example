# =========================
# Core App
# =========================

# postgres://USER:PASSWORD@HOST:PORT/DB?schema=public
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/icn_freeze?schema=public"

# ใช้เซ็น/ตรวจ JWT ฝั่งเซิร์ฟเวอร์ ควรเป็นสตริงสุ่มยาวๆ
JWT_SECRET="replace_with_a_long_random_secret"

# ใช้ฝั่ง client (Next.js) เพื่อสร้างลิงก์ภายใน เช่น callback URL
# สำหรับ dev ให้เป็น http://localhost:3000
NEXT_PUBLIC_BASE_URL="http://localhost:3000"


# =========================
# Google OAuth 2.0
# =========================
# คีย์จาก Google Cloud Console (OAuth client)
GOOGLE_CLIENT_ID="your_google_oauth_client_id"
GOOGLE_CLIENT_SECRET="your_google_oauth_client_secret"

# Redirect URI ต้องตรงกับที่ตั้งไว้ใน Google Cloud
# สำหรับ development:
GOOGLE_OAUTH_REDIRECT="http://localhost:3000/api/auth/google/callback"

# (ตัวเลือก) สำหรับ production: เปลี่ยนโดเมนให้ตรงของจริง แล้วตั้งใน Google Cloud ด้วย
# GOOGLE_OAUTH_REDIRECT="https://your-domain.com/api/auth/google/callback"


# =========================
# Upload / Storage (ตัวเลือก)
# =========================
# โฟลเดอร์เก็บสลิปจะใช้ public/uploads/slips ตามโค้ดอยู่แล้ว
# ถ้าต้องการกำหนดเองสามารถเพิ่มตัวแปรของคุณเอง เช่น:
# UPLOAD_DIR_SLIPS="public/uploads/slips"


# =========================
# i18n / Locale (ตัวเลือก)
# =========================
# ค่าเริ่มต้นในโค้ดรองรับ th และ en อยู่แล้ว
# DEFAULT_LOCALE="th"
